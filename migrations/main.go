package migrations

// Struktur Database:
// CREATE TABLE bobot (
//     id SERIAL PRIMARY KEY,
//     parent_id INT REFERENCES bobot(id),
//     nama VARCHAR(255) NOT NULL,
//     nomor VARCHAR(255) NOT NULL,
//     UNIQUE(nomor)
// );
